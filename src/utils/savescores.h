#ifndef SAVESCORES_H
#define SAVESCORES_H

#include <vector>
#include <string>

int saveScores(const std::vector<std::pair<std::string, unsigned>> & scores, const std::string & fileName);

#endif // SAVESCORES_H
